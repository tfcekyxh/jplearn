# 实施计划 · 五十音速成

> 基于 `project-scope.md`，将开发拆分为可逐个执行的小阶段。每个阶段完成后可独立验证。

---

## 阶段 1：项目骨架搭建

**目标**：`bun run dev` 启动，浏览器看到首页。

| # | 任务 | 细节 |
|---|------|------|
| 1.1 | 初始化项目 | `bun create vite jplearn --template react`，清理模板多余文件 |
| 1.2 | 安装依赖 | 如需 Tailwind、React Router，在此阶段引入 |
| 1.3 | 建立目录结构 | `src/pages/`、`src/components/`、`src/data/`、`src/hooks/`、`src/db/` |
| 1.4 | 首页路由 | 首页展示三个卡片入口（卡片学习 / 假名测验 / 拼读练习），点击可跳转占位页面 |
| 1.5 | 移动端基础样式 | 设置 viewport、全局字体、背景色，确认在 375px 宽度下布局正常 |

**验证**：`bun run dev` → 浏览器打开 → 看到首页三个入口卡片，点击可进入占位页。

---

## 阶段 2：假名数据 + 单词数据

**目标**：所有数据文件就位，可在浏览器 console 中导入查看。

| # | 任务 | 细节 |
|---|------|------|
| 2.1 | 编写 `kanaData.js` | 50 音全表：每个假名包含 `hiragana`、`katakana`、`romaji`、`row`（あ行~わ行）、`mnemonic`（先留空字符串） |
| 2.2 | 编写 `wordData.js` | 20~30 个基础单词：每个词包含 `kana`、`kanji`（如有）、`meaning`（中文意思） |
| 2.3 | 导出结构统一 | 统一 export 格式，确保其他模块可直接 import 使用 |

**验证**：在浏览器 console 中 `import { kanaData } from './data/kanaData'` 能打印完整假名数组。

---

## 阶段 3：卡片学习模块

**目标**：可翻看假名卡片、按行筛选、听发音。

| # | 任务 | 细节 |
|---|------|------|
| 3.1 | 卡片组件 `KanaCard` | 显示当前假名（平假名大字 + 片假名小字）、罗马音、速记口诀（有则显示） |
| 3.2 | 翻卡逻辑 | 「上一张 / 下一张」按钮切换卡片，也支持左右滑动手势（可选） |
| 3.3 | 行筛选器 | 顶部/底部显示行标签（あ、か、さ…），点击筛选只显示该行假名 |
| 3.4 | 发音按钮 | 点击调用 `SpeechSynthesis` API，朗读当前假名 |
| 3.5 | 页面组装 `CardLearningPage` | 组合卡片 + 筛选器 + 导航按钮 |

**验证**：进入卡片学习 → 看到假名卡片 → 能翻卡 → 能筛选 → 能听发音。

---

## 阶段 4：假名测验模块

**目标**：随机出题，用户输入罗马音，判断对错，统计正确率。

| # | 任务 | 细节 |
|---|------|------|
| 4.1 | 出题逻辑 `useQuiz` hook | 从已学假名（先全量，等阶段 6 后改为基于 IndexedDB 学习记录）中随机抽一个，随机平假名或片假名 |
| 4.2 | 答题组件 `QuizPage` | 显示假名 → 输入框 → 提交按钮 |
| 4.3 | 判题 + 反馈 | 比对用户输入与 `romaji`，显示 ✓/✗，附带正确读音 |
| 4.4 | 得分统计 | 当前轮次正确数 / 总数，显示正确率百分比 |

**验证**：进入测验 → 看到随机假名 → 输入罗马音 → 提交后看到对/错 → 分数统计正确。

---

## 阶段 5：拼读练习模块

**目标**：显示单词，用户拼读后标记会/不会，记录错词。

| # | 任务 | 细节 |
|---|------|------|
| 5.1 | 单词卡片组件 `WordCard` | 显示单词假名 + 中文意思 |
| 5.2 | 发音按钮 | 朗读整个单词 |
| 5.3 | 「我会了 / 不会」按钮 | 点击后切换下一词，标记结果 |
| 5.4 | 页面组装 `WordReadingPage` | 组合单词卡片 + 发音 + 标记按钮 |

**验证**：进入拼读练习 → 看到单词 → 能听发音 → 能标记会/不会 → 自动切换到下一个词。

---

## 阶段 6：IndexedDB 数据持久化

**目标**：学习记录和错题存入 IndexedDB，关闭浏览器后不丢失。

| # | 任务 | 细节 |
|---|------|------|
| 6.1 | 封装 IndexedDB 工具 `db.js` | 提供 `saveRecord`、`getLearnedKana`、`getWrongWords`、`getStats` 等接口 |
| 6.2 | 学习记录 | 卡片学习中翻过的假名自动标记为「已学」 |
| 6.3 | 错题记录 | 测验答错的假名、拼读标记「不会」的单词存入 |
| 6.4 | 测验范围联动 | 阶段 4 的出题逻辑改为从 IndexedDB 中「已学假名」抽选 |

**验证**：学习几张卡片 → 关闭浏览器 → 重新打开 → IndexedDB 中学习记录仍在 → 测验范围仅限已学假名。

---

## 阶段 7：样式优化 + 动画

**目标**：UI 整洁，移动端体验良好，有轻量动画。

| # | 任务 | 细节 |
|---|------|------|
| 7.1 | 首页美化 | 三个入口卡片设计感提升，图标/符号装饰 |
| 7.2 | 卡片组件美化 | 圆角、阴影、假名字体放大、配色统一（黑白灰 + 淡蓝点缀） |
| 7.3 | 按钮/触摸优化 | 所有可点击元素 ≥ 44×44px，主要按钮放在屏幕下半部 |
| 7.4 | 过渡动画 | 翻卡切换动画、答题反馈动画（对/错闪烁）、页面切换过渡 |
| 7.5 | 响应式适配 | 375~430px 竖屏最优，平板/PC 也能用 |

**验证**：在手机浏览器中打开，各页面 UI 整洁，动画流畅。

---

## 阶段 8：自测 + 修复

**目标**：全流程走通，修复发现的问题，达到 v1.0 可交付状态。

| # | 任务 | 细节 |
|---|------|------|
| 8.1 | 全流程走查 | 逐个功能走一遍：学卡片 → 测验 → 拼读 → 关浏览器重开 → 数据仍在 |
| 8.2 | 边界情况 | 空数据、全部答对/答错、快速点击、浏览器兼容 |
| 8.3 | 修复问题 | 记录并修复所有发现的问题 |
| 8.4 | 最终确认 | 对照 project-scope 确认清单逐项核对 |

**验证**：八大里程碑全部通过，v1.0 可用。

---

## 依赖关系

```
阶段 1 ──→ 阶段 2 ──→ 阶段 3（卡片学习）
                    ├──→ 阶段 4（假名测验）
                    ├──→ 阶段 5（拼读练习）
                    └──→ 阶段 6（IndexedDB）←── 阶段 4 的测验范围依赖此阶段
                                                    ↓
                                              阶段 7（样式优化）
                                                    ↓
                                              阶段 8（自测修复）
```

---

## 技术约定速查

| 项 | 约定 |
|----|------|
| 包管理器 | Bun |
| 启动命令 | `bun run dev` |
| 框架 | React 函数组件 + Hooks |
| 样式 | Tailwind CSS |
| 存储 | IndexedDB（封装于 `src/db/`） |
| 语音 | Web Speech API (`SpeechSynthesis`) |
| 路由 | React Router，首页三个卡片入口 |
