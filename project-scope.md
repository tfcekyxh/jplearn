# 项目范围文档 · 五十音速成

> 项目代号：`五十音速成` | 更新时间：2026-06-13

---

## 一、项目定位

| 项目 | 内容 |
|---|---|
| 一句话描述 | 一个可以在浏览器中学习五十音的小工具软件 |
| 目标用户 | 零基础日语初学者 |
| 平台 | **移动端优先**的网页 Web 应用（手机浏览器打开，PC 也能用） |
| 版本 | 极简版（v1.0） |

---

## 二、核心功能

### 功能 1：卡片学习（Card Learning）
- 显示假名卡片（平假名 + 片假名）
- 卡片下方展示**速记口诀 / 速记句子**（速记口诀先留空，后续接入 AI 生成）
- 支持翻卡、上一张 / 下一张
- 支持按行筛选（あ行、か行……）
- 点击播放该假名的**发音**（浏览器语音合成 API）

### 功能 2：随机假名识别测验（Quiz）
- 随机显示一个假名（平/片假名，从**已学过的假名**中抽选）
- 用户输入罗马音 → 判断对错
- 答题后显示结果（对/错）+ 正确读音
- 统计正确率，存到 IndexedDB

### 功能 3：拼读练习（Word Reading）
- 显示一个完整的单词（如 `さかな`），带中文意思（鱼）
- 用户拼读 → 可听发音 → 确认"我会了 / 不会"
- 错题/错词记录，便于复习

---

## 三、技术栈

| 层面 | 选型 |
|---|---|
| 前端框架 | **React**（函数组件 + Hooks） |
| 构建工具 | Vite（轻量、启动快） |
| 样式 | CSS / Tailwind（简洁风格） |
| 数据存储 | **IndexedDB**（浏览器本地数据库，持久化学习进度） |
| 语言 | 中文界面 |
| 语音 | 浏览器自带 `Web Speech API`（`SpeechSynthesis`） |
| 包管理器 | **Bun**（bun install / bun run dev） |
| 部署目标 | 本地运行（`npm run dev` 起开发服务器） |

---

## 四、功能模块清单

```
五十音速成（Web App）
├── 首页 / 导航（三个卡片式入口，点击进入对应模块）
│   ├── 卡片学习入口
│   ├── 假名测验入口
│   └── 拼读练习入口
│
├── 模块一：卡片学习
│   ├── 假名数据（从 markdown 迁移过来）
│   ├── 卡片组件（假名 + 罗马音 + 速记口诀）
│   ├── 上一张 / 下一张 / 随机
│   ├── 发音按钮（Web Speech API）
│   └── 按行筛选（あ行~わ行）
│
├── 模块二：假名测验
│   ├── 随机出题（假名 → 罗马音）
│   ├── 输入框 + 提交
│   ├── 即时反馈（对/错）
│   └── 得分 / 正确率统计
│
├── 模块三：拼读练习
│   ├── 单词库（约 20~30 个基础单词）
│   ├── 单词卡片（假名 + 中文意思）
│   ├── 发音按钮
│   └── "我会了 / 不会" 按钮（记录到 IndexedDB）
│
└── 数据层（IndexedDB）
    ├── 学习记录（已学过的假名）
```

---
## 六、设计风格（移动端优先 Mobile-First）

| 项目 | 方案 |
|---|---|
| 风格 | **简洁**，以黑白灰为主，点缀淡蓝色 |
| 布局 | **移动端优先**：默认竖屏单列布局，按钮/卡片适合大拇指操作 |
| 字体 | 系统默认字体，日语假名字符放大显示（卡片上的假名要大而清晰） |
| 卡片 | 圆角 + 柔和阴影，轻触反馈明显 |
| 动画 | 轻量级过渡效果（翻卡、按钮反馈） |
| 响应式 | **主要目标**：手机竖屏（375~430px 宽）；兼容：平板/PC |
| 交互区域 | 所有主要按钮放在屏幕下半部分（单手可及区） |
| 触摸目标 | 按钮/可点击元素最小 44×44px（苹果推荐标准） |

---

## 七、非功能要求（隐含约束）

| 项目 | 内容 |
|---|---|
| 离线可用 | 纯前端 + IndexedDB，不依赖后端 |
| 数据安全 | 所有数据存浏览器，不上传任何服务器 |
| 性能 | 页面首次加载 < 2 秒（本地开发环境） |
| 浏览器兼容性 | 现代浏览器（Chrome / Edge / Firefox / Safari 最新版） |

---

## 八、开发里程碑

| 阶段 | 内容 | 产出 |
|---|---|---|
| Step 1 | 搭建 React + Vite 项目骨架，建立目录结构 | 项目可以 `bun run dev` 启动并显示首页 |
| Step 2 | 准备假名数据（`kanaData.js`）+ 单词数据（`wordData.js`） | 所有假名和口诀数据就位 |
| Step 3 | 实现【卡片学习】功能 | 可以翻卡、按行筛选、听发音 |
| Step 4 | 实现【假名测验】功能 | 随机出题 + 判断对错 + 统计得分 |
| Step 5 | 实现【拼读练习】功能 | 单词库 + 发音 + 会/不会标记 |
| Step 6 | 封装 IndexedDB，保存学习进度和错题 | 关闭浏览器后再打开，数据仍在 |
| Step 7 | 样式优化 + 响应式 + 简单动画 | UI 整洁可交付 |
| Step 8 | 自测 + 修复问题 | v1.0 可使用版本 |

---

## 九、后续可扩展方向（v2 及以后，本期不做）

- [ ] 片假名专门练习
- [ ] 浊音 / 拗音专项练习
- [ ] 平假名 ↔ 片假名 转换练习
- [ ] 学习曲线 / 统计图
- [ ] 导出学习记录为 JSON
- [ ] 多语言界面（中英日切换）

---

## 十、确认清单（请确认 / 指出修改）

- [✅] 技术栈：React + Vite + IndexedDB（中文界面）
- [✅] 三大核心功能：卡片学习 / 假名测验 / 拼读练习
- [✅] 假名数据自行编写，速记口诀后续接入 AI 生成
- [✅] 测验范围：从已学过的假名中抽选（基于 IndexedDB 学习记录）
- [✅] 导航方式：首页三个卡片式入口
- [✅] 包管理器：Bun（bun install / bun run dev）

> 如果以上内容无误，回复"开始"即可进入代码开发阶段。
> 如果需要调整，请指出具体哪一项的修改方案。
